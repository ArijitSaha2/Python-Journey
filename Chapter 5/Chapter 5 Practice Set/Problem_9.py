# Can you change the values inside a list which is contained in S?

s = {8, 7, 12 , "Harry", [1,2]}

# No we can't change values inside this set as the list [1,2] will give us an error.
# TypeError: unhashable type: 'list' prevents set creation, thus no changes possible within.