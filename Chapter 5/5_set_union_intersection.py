s1 = {1,3,4,5}
s2 = {1,5,6,2}

print(s1.union(s2)) # will print all elements in sets except won't repeat the same elements as the property states

print(s1.intersection(s2)) # will print only the elements present in both sets
